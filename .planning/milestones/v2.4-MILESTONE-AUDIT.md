---
milestone: v2.4
audited: 2026-04-07
status: tech_debt
scores:
  requirements: 16/16
  phases: 3/3
  integration: 5/5
  flows: 6/6
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 27-repo-exploration-tasks
    items:
      - "REPL investigation tasks render a redundant status box (`renderResultBlock`) after the inline report — cosmetic UX duplication. Fix: skip render when `intent.taskType === 'investigation'` in src/cli/commands/repl.ts:367 or return `result: null` from processInput. (Finding 1)"
      - "CLI `run` command (src/cli/commands/run.ts) does not pass `explorationSubtype` to AgentOptions and lacks an `--task-type investigation` guard analogous to the existing `generic` guard. CLI investigation runs always fall back to subtype `general`. Not a crash; capability gap. (Finding 10)"
  - phase: cross-cutting
    items:
      - "SUMMARY.md `requirements-completed` frontmatter is sparse: 25-01, 26-02, 27-01, 27-02 list zero requirements despite their VERIFICATION.md tables marking REQs as SATISFIED. Recommend backfilling SUMMARY frontmatter on next pass for traceability hygiene."
nyquist:
  compliant_phases: 0
  partial_phases: 2  # 26, 27 — VALIDATION.md exists, nyquist_compliant: false
  missing_phases: 1  # 25 — no VALIDATION.md
  overall: partial
---

# Milestone v2.4 Audit — Git Worktree & Repo Exploration

**Audited:** 2026-04-07
**Status:** tech_debt (no blockers; accumulated minor items + Nyquist coverage gaps)

## Scope

| Phase | Goal | Status |
|-------|------|--------|
| 25 — Tech Debt Cleanup | Clean baseline before feature work | passed (7/7) |
| 26 — Git Worktree Isolation | Per-session worktrees, Docker mounts worktree | passed (4/4) |
| 27 — Repo Exploration Tasks | Investigation task type, read-only enforcement | passed (13/13) |

## Requirements Coverage (16/16)

3-source cross-reference (VERIFICATION.md tables + SUMMARY frontmatter + REQUIREMENTS.md traceability):

| REQ-ID | Phase | VERIFICATION | SUMMARY frontmatter | Traceability `[x]` | Final |
|--------|-------|--------------|---------------------|--------------------|-------|
| DEBT-01 | 25 | passed | missing | yes | satisfied |
| DEBT-02 | 25 | passed | missing | yes | satisfied |
| DEBT-03 | 25 | passed | missing | yes | satisfied |
| DEBT-04 | 25 | passed | listed (25-02) | yes | satisfied |
| DEBT-05 | 25 | passed | listed (25-02) | yes | satisfied |
| DEBT-06 | 25 | passed | listed (25-02) | yes | satisfied |
| WKTREE-01 | 26 | passed | listed (26-01) | yes | satisfied |
| WKTREE-02 | 26 | passed | missing | yes | satisfied |
| WKTREE-03 | 26 | passed | listed (26-01) | yes | satisfied |
| WKTREE-04 | 26 | passed | listed (26-01) | yes | satisfied |
| WKTREE-05 | 26 | passed | missing | yes | satisfied |
| EXPLR-01 | 27 | passed | missing | yes | satisfied |
| EXPLR-02 | 27 | passed | missing | yes | satisfied |
| EXPLR-03 | 27 | passed | missing | yes | satisfied |
| EXPLR-04 | 27 | passed | missing | yes | satisfied |
| EXPLR-05 | 27 | passed | listed (27-03) | yes | satisfied |

**Orphans:** none. Every REQ-ID in REQUIREMENTS.md traceability table maps to a VERIFICATION.md entry marked SATISFIED. Authoritative source: VERIFICATION.md tables (which are detailed and evidence-backed); SUMMARY frontmatter is sparse but does not contradict VERIFICATION.

## Cross-Phase Integration

All 10 integration findings from the integration checker resolve to WIRED with two non-blocking observations:

- **DEBT-04 ↔ WKTREE-02:** retry.ts configOnly verifier operates on `effectiveWorkspaceDir` (worktree path). No conflict.
- **DEBT-03 ↔ WKTREE-03:** Cancelled tasks correctly record `'cancelled'` in history AND remove the worktree in `finally`. Both throw and non-throw paths covered.
- **EXPLR-03 ↔ WKTREE-01:** Investigation bypass in `runAgent` returns before worktree creation; `worktreeManager` stays null; finally is a no-op.
- **WKTREE-04 + EXPLR-01:** REPL `pruneOrphans` runs at startup unconditionally, regardless of upcoming task type.
- **WKTREE-02 + EXPLR-04:** Docker mount path: regular tasks → worktree `:rw`; investigation → main repo `:ro`. `readOnly` flag flows via `SessionConfig.readOnly` through both paths.
- **DEBT-06 + EXPLR-05:** Slack adapter's `appendHistory` finally block correctly records investigation entries with description; `createPr` guard at adapter.ts:144 + agentOptions:194 (double-guard).
- **WKTREE-05 cross-cut:** `state.lastWorktreeBranch` is never set for investigation runs (correct — no worktree exists).

## E2E Flows (6/6)

| Flow | Status |
|------|--------|
| A. Concurrent dep-update tasks → two worktrees, two PRs | complete |
| B. REPL exploration → no worktree, :ro mount, inline report, no PR | complete (cosmetic: duplicate status box, see tech debt) |
| C. Exploration with "save it" → `.reports/<ts>-<subtype>.md` | complete |
| D. Slack exploration → createPr false, thread report, history appended | complete |
| E. REPL crash → restart pruneOrphans removes stale worktree | complete |
| F. Cancelled task → worktree removed AND `cancelled` in history | complete |

## Tech Debt

### Phase 27
- **Cosmetic — REPL renders status box after investigation report.** `processInput` returns the full `RetryResult` unconditionally (`src/repl/session.ts:305`), and `src/cli/commands/repl.ts:367` renders any truthy result. The report is printed correctly, but is followed by a "Status: success / Verify: N/A / Judge: N/A" box that is irrelevant for investigation. Fix: guard `renderResultBlock` on `intent.taskType !== 'investigation'`.
- **CLI `run` command lacks investigation subtype support.** `src/cli/commands/run.ts` (~lines 75-86) does not forward `explorationSubtype` and has no investigation guard equivalent to the existing `generic` guard. CLI investigation runs default to subtype `general`. REPL/Slack are unaffected.

### Cross-cutting
- **SUMMARY.md frontmatter sparse.** Plans 25-01, 26-02, 27-01, 27-02 list zero REQs in `requirements-completed` despite their VERIFICATION.md tables marking REQs as SATISFIED. Backfill recommended for traceability hygiene.

## Nyquist Compliance

| Phase | VALIDATION.md | nyquist_compliant | Status |
|-------|---------------|-------------------|--------|
| 25 — Tech Debt Cleanup | missing | n/a | MISSING |
| 26 — Git Worktree Isolation | exists | false | PARTIAL |
| 27 — Repo Exploration Tasks | exists | false | PARTIAL |

Overall: **partial**. Phase 25 has no validation strategy file. Phases 26 and 27 have draft VALIDATION.md but `nyquist_compliant: false` and `wave_0_complete: false`. Run `/gsd:validate-phase 25`, `/gsd:validate-phase 26`, `/gsd:validate-phase 27` to close coverage.

## Verdict

All 16 milestone requirements are SATISFIED with cross-phase wiring verified end-to-end. No blocking gaps. Tech debt items are minor (one cosmetic UX issue, one CLI capability gap, one frontmatter hygiene cleanup) and Nyquist validation coverage is incomplete across all 3 phases.

Status: **tech_debt** — milestone is functionally complete; review and decide whether to address debt before completing.
