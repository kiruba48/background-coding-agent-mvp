---
phase: 18-intent-parser-generalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/intent/fast-path.ts
  - src/intent/fast-path.test.ts
autonomous: true
requirements:
  - INTENT-02
  - INTENT-03

must_haves:
  truths:
    - "fastPathParse('replace axios with fetch') returns null (not a dep update match)"
    - "fastPathParse('rename getUserData to fetchUserProfile') returns null"
    - "fastPathParse('update recharts') still returns a dep update result (verb guard does not block dep verbs)"
    - "@anthropic-ai/sdk is at ^0.80.0 in package.json so GA structured outputs API is available"
  artifacts:
    - path: "package.json"
      provides: "SDK version bump to ^0.80.0"
      contains: '"@anthropic-ai/sdk": "^0.80.0"'
    - path: "src/intent/fast-path.ts"
      provides: "Verb guard regex as first check in fastPathParse()"
      contains: "REFACTORING_VERB_GUARD"
    - path: "src/intent/fast-path.test.ts"
      provides: "Test cases for verb guard"
      contains: "replace axios with fetch"
  key_links:
    - from: "src/intent/fast-path.ts"
      to: "src/intent/index.ts"
      via: "fastPathParse returns null for refactoring verbs, forcing LLM path"
      pattern: "REFACTORING_VERB_GUARD"
---

<objective>
Bump the Anthropic SDK to ^0.80.0 (prerequisite for GA structured outputs migration in Plan 02) and add the refactoring verb guard to the fast-path parser so that instructions like "replace axios with fetch" are never misclassified as dependency updates.

Purpose: INTENT-02 prevents false-positive dep-update matches for refactoring instructions. SDK bump unblocks INTENT-03 in Plan 02.
Output: Updated package.json with SDK ^0.80.0, fast-path.ts with verb guard, passing tests.
</objective>

<execution_context>
@/Users/kiruba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kiruba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-intent-parser-generalization/18-CONTEXT.md
@.planning/phases/18-intent-parser-generalization/18-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/intent/types.ts:
```typescript
export interface FastPathResult {
  dep: string;
  version: string;
  project: string | null;
  createPr: boolean;
  isFollowUp?: boolean;
}
```

From src/intent/fast-path.ts:
```typescript
export function fastPathParse(input: string): FastPathResult | null;
// Returns null when input doesn't match any dep-update pattern.
// Verb guard will follow this same protocol — return null to force LLM classification.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump @anthropic-ai/sdk to ^0.80.0</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current SDK version is ^0.71.2 on line ~29)
  </read_first>
  <action>
Run `npm install @anthropic-ai/sdk@^0.80.0` to bump the SDK from ^0.71.2 to ^0.80.0.

After install, verify the TypeScript compiler accepts the new SDK by running `npx tsc --noEmit`. The existing code uses `client.beta.messages.create()` which still exists in 0.80.0, so compilation should pass without changes.

Do NOT modify any source files in this task — just the dependency bump.
  </action>
  <verify>
    <automated>grep '"@anthropic-ai/sdk"' package.json | grep -q '0.80' && npx tsc --noEmit && echo "SDK bump OK"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"@anthropic-ai/sdk": "^0.80.0"` (or compatible ^0.80.x)
    - `npx tsc --noEmit` exits 0 (no type errors introduced)
    - `npm test` passes (existing tests still work with new SDK)
  </acceptance_criteria>
  <done>SDK bumped to ^0.80.0, all existing tests and type checks pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add refactoring verb guard to fastPathParse()</name>
  <files>src/intent/fast-path.ts, src/intent/fast-path.test.ts</files>
  <read_first>
    - src/intent/fast-path.ts (current fastPathParse implementation, insertion point after line 32 empty-string guard)
    - src/intent/fast-path.test.ts (existing test structure, append new describe block)
  </read_first>
  <behavior>
    - `fastPathParse('replace axios with fetch')` returns `null`
    - `fastPathParse('rename getUserData to fetchUserProfile')` returns `null`
    - `fastPathParse('move utils to shared')` returns `null`
    - `fastPathParse('extract helper function')` returns `null`
    - `fastPathParse('migrate from jest to vitest')` returns `null`
    - `fastPathParse('rewrite auth module')` returns `null`
    - `fastPathParse('REPLACE axios with fetch')` returns `null` (case insensitive)
    - `fastPathParse('replace axios with fetch and create PR')` returns `null` (verb guard fires before PR suffix strip)
    - `fastPathParse('update recharts')` still returns `{ dep: 'recharts', version: 'latest', ... }` (dep verbs NOT blocked)
    - `fastPathParse('update axios')` still returns dep result (not blocked even though "axios" is a dep name)
    - `fastPathParse('replace')` without trailing space returns `null` from the existing no-match path (regex requires `\s` after verb) — actually this won't match the verb guard so it falls through to existing patterns which also won't match, returning null. Either way null is correct.
  </behavior>
  <action>
1. Add tests FIRST in `src/intent/fast-path.test.ts` — create a new `describe('verb guard')` block after the existing `describe('fastPathParse')` block with the test cases from the behavior section above.

2. Run tests to confirm they fail (verb guard not yet implemented).

3. Add the verb guard to `src/intent/fast-path.ts`. Insert these lines immediately after the empty-string guard (`if (!trimmed) return null;`) on line 32, BEFORE the PR suffix strip on line 35:

```typescript
  // Verb guard: refactoring instructions must go through LLM classification,
  // not be misclassified as dependency updates by DEPENDENCY_PATTERNS
  const REFACTORING_VERB_GUARD = /^(?:replace|rename|move|extract|migrate|rewrite)\s/i;
  if (REFACTORING_VERB_GUARD.test(trimmed)) return null;
```

The six verbs are: replace, rename, move, extract, migrate, rewrite. The regex requires a whitespace character after the verb (`\s`) so bare "replace" without a space won't match (it falls through to existing patterns which also won't match, returning null correctly).

Move the `REFACTORING_VERB_GUARD` const to module scope (alongside FOLLOW_UP_PATTERNS and DEPENDENCY_PATTERNS) for consistency with the existing pattern of module-level regex constants. Export it for test visibility if desired, but not required.

4. Run tests to confirm they pass.
  </action>
  <verify>
    <automated>npx vitest run src/intent/fast-path.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `fast-path.ts` contains `REFACTORING_VERB_GUARD = /^(?:replace|rename|move|extract|migrate|rewrite)\s/i`
    - The verb guard check (`if (REFACTORING_VERB_GUARD.test(trimmed)) return null;`) appears BEFORE the `PR_SUFFIX.test(trimmed)` line
    - `fast-path.test.ts` contains a `describe('verb guard')` block with at least 8 test cases
    - `fast-path.test.ts` includes a test that `'update recharts'` still returns a dep result (regression guard)
    - `npx vitest run src/intent/fast-path.test.ts` exits 0
  </acceptance_criteria>
  <done>Verb guard blocks all six refactoring verbs from fast-path, existing dep-update patterns still work, all tests pass</done>
</task>

</tasks>

<verification>
npx vitest run src/intent/ && npx tsc --noEmit
</verification>

<success_criteria>
- SDK bumped to ^0.80.0 with no regressions
- Verb guard correctly returns null for all six refactoring verbs
- Existing dep-update fast-path tests still pass
- Full intent test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/18-intent-parser-generalization/18-01-SUMMARY.md`
</output>
