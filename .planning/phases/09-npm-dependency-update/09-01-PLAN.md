---
phase: 09-npm-dependency-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/prompts/npm.ts
  - src/prompts/npm.test.ts
  - src/prompts/index.ts
  - src/cli/index.ts
autonomous: true
requirements: [NPM-01, NPM-05]

must_haves:
  truths:
    - "CLI accepts --task-type npm-dependency-update with --dep and --target-version flags"
    - "npm --dep validation is minimal (non-empty, no control chars) unlike Maven's strict groupId:artifactId"
    - "buildPrompt dispatches npm-dependency-update to buildNpmPrompt"
    - "npm prompt uses end-state format (desired outcome, not steps)"
    - "NPM-05 (changelog link) is documented as deferred in the prompt (same as MVN-05)"
  artifacts:
    - path: "src/prompts/npm.ts"
      provides: "buildNpmPrompt end-state prompt builder"
      exports: ["buildNpmPrompt"]
    - path: "src/prompts/npm.test.ts"
      provides: "Unit tests for npm prompt builder and dispatch"
      min_lines: 30
    - path: "src/prompts/index.ts"
      provides: "Updated buildPrompt with npm-dependency-update case"
      exports: ["buildPrompt", "buildNpmPrompt"]
    - path: "src/cli/index.ts"
      provides: "npm-dependency-update in depRequiringTaskTypes with minimal validation"
  key_links:
    - from: "src/cli/index.ts"
      to: "src/prompts/index.ts"
      via: "depRequiringTaskTypes includes npm-dependency-update; buildPrompt called in run.ts"
      pattern: "depRequiringTaskTypes.*npm-dependency-update"
    - from: "src/prompts/index.ts"
      to: "src/prompts/npm.ts"
      via: "switch case dispatches to buildNpmPrompt"
      pattern: "case 'npm-dependency-update'"
---

<objective>
Create the npm prompt builder and wire npm-dependency-update into the CLI validation pipeline.

Purpose: Enable `--task-type npm-dependency-update` with proper --dep/--target-version validation and end-state prompt generation, mirroring the Maven pattern established in Phase 8.
Output: src/prompts/npm.ts, updated src/prompts/index.ts dispatch, updated src/cli/index.ts validation
</objective>

<execution_context>
@/Users/kiruba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kiruba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-npm-dependency-update/09-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/prompts/index.ts:
```typescript
export interface PromptOptions {
  taskType: string;
  dep?: string;
  targetVersion?: string;
}
export function buildPrompt(options: PromptOptions): string;
```

From src/prompts/maven.ts (template to follow):
```typescript
export function buildMavenPrompt(dep: string, targetVersion: string): string;
```

From src/cli/index.ts (line 65):
```typescript
const depRequiringTaskTypes = ['maven-dependency-update'];
// Line 76: Maven validation uses strict regex /^[a-zA-Z0-9._-]+:[a-zA-Z0-9._-]+$/
// npm validation should be DIFFERENT: minimal (non-empty, no control chars/whitespace)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create npm prompt builder with tests</name>
  <files>src/prompts/npm.ts, src/prompts/npm.test.ts</files>
  <behavior>
    - buildNpmPrompt('lodash', '5.0.0') contains 'lodash' and '5.0.0'
    - buildNpmPrompt output mentions package.json (not pom.xml)
    - buildNpmPrompt describes end-state: build succeeds, tests pass
    - buildNpmPrompt mentions fixing breaking API changes
    - buildNpmPrompt does NOT contain step-by-step instructions (no "Step 1:", no numbered steps)
    - buildNpmPrompt includes "Work in the current directory"
    - buildNpmPrompt does NOT mention lockfile (agent only edits package.json; host handles lockfile per CONTEXT.md)
    - buildPrompt dispatches 'npm-dependency-update' to buildNpmPrompt
    - buildPrompt throws when npm-dependency-update is missing dep
    - buildPrompt throws when npm-dependency-update is missing targetVersion
  </behavior>
  <action>
    Create src/prompts/npm.ts following the exact pattern of src/prompts/maven.ts:
    - Export buildNpmPrompt(packageName: string, targetVersion: string): string
    - End-state prompt: "Update the npm package {name} to version {version}"
    - Desired state: package.json references target version, build succeeds, tests pass, breaking API changes resolved
    - Do NOT mention lockfile regeneration in the prompt (host-side concern, not agent concern)
    - NPM-05 (changelog link): Note this is deferred (no network in Docker). Do NOT add changelog instructions to the prompt.

    Create src/prompts/npm.test.ts mirroring src/prompts/maven.test.ts structure:
    - Tests for buildNpmPrompt content, end-state format, no step-by-step
    - Tests for buildPrompt dispatch: npm-dependency-update case, missing dep/targetVersion throws

    Update src/prompts/index.ts:
    - Import buildNpmPrompt from './npm.js'
    - Export buildNpmPrompt
    - Add 'npm-dependency-update' case to buildPrompt switch with same dep/targetVersion validation as Maven case
  </action>
  <verify>
    <automated>cd /Users/kiruba/code/Projects/ai/background-coding-agent && npx vitest run src/prompts/npm.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>buildNpmPrompt creates end-state prompt for npm dependency updates; buildPrompt dispatches npm-dependency-update correctly; all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Add npm-dependency-update to CLI validation</name>
  <files>src/cli/index.ts</files>
  <action>
    Update src/cli/index.ts:

    1. Add 'npm-dependency-update' to the depRequiringTaskTypes array (line 65):
       `const depRequiringTaskTypes = ['maven-dependency-update', 'npm-dependency-update'];`

    2. After the existing Maven --dep format validation block (lines 75-80), add a task-type-aware validation branch:
       - If taskType is 'maven-dependency-update': keep existing strict groupId:artifactId regex
       - If taskType is 'npm-dependency-update': minimal validation only -- non-empty (already guaranteed by the depRequiringTaskTypes check above), no control characters, no whitespace. Use regex: /[\x00-\x1f\s]/ to reject. Per CONTEXT.md: "Minimal validation for npm: non-empty, no control characters or whitespace -- don't enforce npm naming rules"

    3. Update the --dep option description (line 20) to show both formats:
       `'Dependency to update (e.g., org.springframework:spring-core for Maven, lodash for npm)'`

    Restructure the validation block so Maven's strict regex only runs for maven-dependency-update, not for npm. The current code applies the depPattern universally -- this must become task-type-conditional.

    Do NOT change --target-version validation (it already works for both: alphanumeric, dots, hyphens, underscores, plus signs).
  </action>
  <verify>
    <automated>cd /Users/kiruba/code/Projects/ai/background-coding-agent && npx tsc --noEmit 2>&1 | tail -5 && npx vitest run src/prompts/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>npm-dependency-update accepted by CLI with --dep 'lodash' --target-version '5.0.0'; Maven validation still enforces groupId:artifactId; --dep help text shows both formats</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes (no type errors)
2. `npx vitest run src/prompts/` all tests pass (existing Maven + new npm)
3. Manual spot check: CLI help shows updated --dep description
</verification>

<success_criteria>
- buildNpmPrompt generates end-state prompt for npm dependency updates
- buildPrompt dispatches npm-dependency-update to buildNpmPrompt
- CLI accepts npm-dependency-update with --dep (minimal validation) and --target-version
- Maven CLI validation unchanged (strict groupId:artifactId)
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-npm-dependency-update/09-01-SUMMARY.md`
</output>
